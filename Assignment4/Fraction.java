/**
*
* Using your previous Fraction class, add the following methods to the class
*  -Fraction add(Fraction f)
*  -Fraction subtract(Fraction f)
*  -Fraction multiply(Fraction f)
*  -Fraction divide(Fraction f)
*  -int compareTo(Fraction f)
*
**/
public class Fraction{
   private int numerator;
   private int denominator;
   
   public Fraction(int n, int d){
      //TODO: Update this constructor from previous assignments to also throw
      //      IllegalArgumentException with the message "Cannot have 0 in denominator."
   }
   
   public Fraction(int n){
      //TODO: Finish this constructor so that n is over 1 for a whole number
   }
   
   /*
     TODO: create the required method so that when 
     a Fraction object is part of a print() then output
     is numerator/denominator.
   */
   //public...
      
}